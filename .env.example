# AWS S3
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
AWS_BUCKET_NAME=

# Azure Blob Storage
AZURE_CONNECTION_STRING=
AZURE_CONTAINER_NAME=

# Google Cloud Storage
GCS_PROJECT_ID=
GCS_BUCKET_NAME=
GCS_SERVICE_ACCOUNT_JSON_PATH=

# Active Provider - values: aws | azure | gcs
ACTIVE_PROVIDER=aws

# JWT (used from M3 — not active yet)
JWT_SECRET=
JWT_EXPIRY_HOURS=24
